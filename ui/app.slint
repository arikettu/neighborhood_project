import { VerticalBox, Button } from "std-widgets.slint";

export component App inherits Window {
    callback close;

    VerticalBox {
        Text {
            text: "Hello World!";
        }
        Button {
            text: "Close";
            clicked => { close(); }
        }
    }
}